<!-- Short, focused PR template for this profile repo -->

### Summary

What changed and why? Keep this to 1-2 short sentences.

### Checklist

- [ ] I updated `README.md` or added a new project folder with a minimal manifest (`package.json` / `pyproject.toml`).
- [ ] Small, focused commits with imperative messages.

### If this PR adds CI/infra

If this PR adds any files under `.github/workflows/`, `Dockerfile`, or infra manifests, add the `allow-ci` label and request an explicit review from the repo owner before merging.

---
Add a short verification step and any manual checks reviewers should perform (rendered README preview, asset size check, etc.).
